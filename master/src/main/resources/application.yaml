server:
  port: 8080

spring:
  application:
    name: axile
  mvc:
    servlet:
      path: /api/${spring.application.name}

management:
  endpoints:
    web:
      exposure:
        include: [ "health" ]